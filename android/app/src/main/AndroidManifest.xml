<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+: sin este permiso no se puede mostrar NINGUNA notificación,
         incluida la de alerta. Se pide en runtime (ver requestNotificationPermission). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Deja que la notificación de alerta abra la Activity encima del lock screen. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Mostrar la alerta como foreground service (notifee asForegroundService,
         ver displayAlertNotification.ts): mantiene el proceso vivo con
         prioridad elevada durante los primeros minutos tras recibir el push,
         justo la ventana en la que algunos fabricantes (Samsung entre otros)
         matan el proceso antes de que el fullScreenAction llegue a
         dispararse — ver historial de pruebas con push que llegaban tarde o
         nunca. El tipo (shortService) ya viene declarado por notifee-core en
         su propio manifest, no hace falta declararlo acá. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Ubicación al momento de responder "Asistir"/"No asistir". -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:networkSecurityConfig="@xml/network_security_config"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
